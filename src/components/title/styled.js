import styled from 'styled-components';

const TextTitle = styled.div`
  color: var(--clr-blue);
  font-weight: bold;
  font-size: 18px;
  margin: 5px 0;
  @media screen and (max-width: 400px) {
    font-size: 14px;
  }
`;

export { TextTitle };
